kind: pipeline
name: default

steps:
- name: build server
  image: plugins/docker
  settings:
    context: server
    auto_tag: true
    dockerfile: server/Dockerfile
- name: build ui  
  image: plugins/docker
  settings:
    context: ui
    auto_tag: true
    dockerfile: ui/Dockerfile
