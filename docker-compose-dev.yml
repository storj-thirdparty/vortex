version: "3.8"

volumes:
  mongo-data:


services:
  vortex:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    links:
      - "mongo:mongo"
      - "redis:redis"
    command: [ "/bin/bash", "-c", "npm -g install nodemon; npm run dev & nodemon index.js" ]
    volumes:
      - ./:/usr/src/app
  mongo:
    image: mongo:latest
    volumes:
      - mongo-data:/etc/mongo

  redis:
    image: redis:latest
